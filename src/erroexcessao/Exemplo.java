package erroexcessao;

public class Exemplo {

	public static void main(String[] args) {
		/*
		FileReader arquivo = new FileReader("teste.txt");  
		BufferedReader entrada = new BufferedReader(file);  
		
		// Ler e imprimir 3 linhas de teste.txt"  
		for (int i = 0; i < 3; i++)   
		System.out.println(entrada.readLine());  
		fileInput.close(); 
		
		ESSE CÓDIGO NÃO FUNCIONARÁ POIS
		classe FileReader lança uma exceção IOException do tipo checked
	    será necessário utilizar bloco try/catch ou assinatura do método explicitar que 
	    a exceção pode ser lançada.
	    */

		/*
	    int dividendo = 0; 
	    int divisor = 10; 
	    int resultado = dividendo/divisor; 
	    Esse código irá gerar uma RunTimeException do tipo java.lang.ArithmeticException.
	SÃO EXCEÇÕES NÃO CHECADAS 
	*/
	}

}
